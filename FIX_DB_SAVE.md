# Исправление проблемы сохранения в БД

## Проблема
После вызова `/api/crawl` данные не сохранялись в БД, хотя JSON файлы создавались.

## Причина
В схеме Prisma не было уникального индекса на поле `id_post`, из-за чего `skipDuplicates: true` в `createManyTasks` не работал корректно.

## Исправления

1. ✅ Добавлен `@unique` к полю `id_post` в схеме Prisma
2. ✅ Добавлено подробное логирование процесса сохранения
3. ✅ Добавлена проверка подключения к БД перед началом скрапинга

## Что нужно сделать

### 1. Создать миграцию для добавления уникального индекса

```bash
npm run prisma:migrate
```

Или:

```bash
npx prisma migrate dev --name add_unique_id_post
```

### 2. Перегенерировать Prisma клиент

```bash
npm run prisma:generate
```

### 3. Проверить подключение к БД

Убедитесь, что в `.env` файле установлен правильный `DATABASE_URL`:

```
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"
```

### 4. Протестировать

После выполнения миграции попробуйте снова вызвать `/api/crawl`. Теперь в логах вы увидите:
- Проверку подключения к БД
- Количество существующих задач
- Процесс сохранения каждой партии
- Количество сохраненных и пропущенных задач

## Логирование

Теперь в консоли вы увидите:
- `ℹ️  Database connection OK. Current tasks in DB: X` - проверка подключения
- `📝 Attempting to save X tasks to database...` - начало сохранения
- `✅ Saved to DB: X, Duplicates: Y` - результат сохранения
- `⚠️  Warning: X posts were not saved. All might be duplicates.` - если все были дубликатами
- `❌ Database error while saving tasks:` - если произошла ошибка

## Если проблема осталась

1. Проверьте логи сервера на наличие ошибок БД
2. Убедитесь, что миграция выполнена успешно
3. Проверьте, что Prisma клиент перегенерирован
4. Проверьте права доступа к БД
